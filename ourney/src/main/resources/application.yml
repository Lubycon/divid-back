spring:
  datasource:
    url: jdbc:mysql://rds엔드포인트:포트명/database이름
    username: db계정
    password: db계정 비밀번호
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: none

logging.level:
  org.hibernate.SQL: debug

jwt:
  issuer: 이슈
  secret: 시크릿키